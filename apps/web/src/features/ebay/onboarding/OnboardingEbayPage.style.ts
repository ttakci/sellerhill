/**
 * OnboardingEbayPage Styles
 *
 * Rendered inside AppLayout (sidebar + breadcrumb). The connect card itself
 * (width, centering, icon, actions) lives entirely in the shared
 * ConnectEbayPrompt molecule — this file only owns the page shell.
 */

import { PageContainer } from '@repo/ui';

export const Container = PageContainer;
