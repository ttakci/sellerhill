/**
 * Same chrome as the seller shell — see `layouts/shell/AppShell.style.ts`.
 * The operator console differs in what it navigates to, not in how it looks,
 * so it must not fork the shell's surfaces, spacing or focus rings.
 */
export * from '../shell/AppShell.style';
