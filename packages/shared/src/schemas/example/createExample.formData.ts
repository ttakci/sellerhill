/**
 * Create Example Form Data Type
 * 
 * Purpose:
 * - Type definition for the "Create Example" form
 * - Used by web/mobile apps
 * - Separate from API DTOs (domain layer)
 */

export type CreateExampleFormData = {
  name: string;
};
